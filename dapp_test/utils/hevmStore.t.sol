// SPDX-License-Identifier: UNLICENSED
pragma solidity 0.7.5;
import "./RomeSetup.sol";

// contract hevmStore is RomeTest {

//     function test_setFRAXBalance() public {
//         IERC20 token = FRAX;
//         address account = hevm.addr(1);
//         uint256 amount = 100 * 1e18; // 100 FRAX

//         // Set the balance
//         setFRAXBalance(account, amount);

//         // Check the balance
//         uint256 balance = token.balanceOf(account);

//         // Make sure it's updated
//         assertEq(amount, balance);
//     }

//     function test_setDAIBalance() public {
//         IERC20 token = DAI;
//         address account = hevm.addr(1);
//         uint256 amount = 100 * 1e18; // 100 DAI

//         // Set the balance
//         setDAIBalance(account, amount);

//         // Check the balance
//         uint256 balance = token.balanceOf(account);

//         // Make sure it's updated
//         assertEq(amount, balance);
//     }

//     function test_setMIMBalance() public {
//         IERC20 token = MIM;
//         address account = hevm.addr(1);
//         uint256 amount = 100 * 1e18; // 100 MIM

//         // Set the balance
//         setMIMBalance(account, amount);

//         // Check the balance
//         uint256 balance = token.balanceOf(account);

//         // Make sure it's updated
//         assertEq(amount, balance);
//     }
// }
